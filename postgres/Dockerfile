FROM postgres:16-alpine

# Copie le schéma de base (tables vides)
# Le script 01_schema.sql est monté via le volume dans docker-compose.yml

# Les données seront générées par db-seeder et montées via volume
